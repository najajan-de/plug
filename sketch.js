function setup() {
    // createCanvas(400, 400, SVG);
    // background(255);

    // stroke(150);
    // strokeWeight(0.5);

    // for (let i=0; i<40;i++) {
    //     line(i*10,0,i*10,400);
    //     line(0,i*10,400,i*10);
    // }

    // stroke(0);
    // strokeWeight(1);
    // rect(0,0,100,100)
    // // translate(50*sqrt(2)*sin(PI/4),-50*sqrt(2)*(1-cos(PI/4)))
    // // rotate(QUARTER_PI)
    // textSize(26)
    // textAlign(CENTER,CENTER)
    // textFont("Impact");
    // text("Jeder trinkt\n1",0,0,100,100)

  }

  function draw() {

  }

  function setLineDash(list) {
    drawingContext.setLineDash(list);
  }